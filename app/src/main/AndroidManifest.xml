<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_ASSISTED_GPS" />
    <uses-permission android:name="android.permission.ACCESS_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:hardwareAccelerated="true">
        <activity
            android:name="com.whatsberry.xmpp.MainActivity"
            android:theme="@android:style/Theme.Light"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.whatsberry.xmpp.QRCodeActivity"
            android:theme="@android:style/Theme.Light"
            android:exported="false"/>

        <activity
            android:name="com.whatsberry.xmpp.MainTabsActivity"
            android:theme="@android:style/Theme.Light.NoTitleBar"
            android:exported="false"/>

        <activity
            android:name="com.whatsberry.xmpp.ContactsActivity"
            android:theme="@android:style/Theme.Light"
            android:exported="false"/>

        <activity
            android:name="com.whatsberry.xmpp.ChatActivity"
            android:theme="@android:style/Theme.Light.NoTitleBar"
            android:exported="false"/>

        <activity
            android:name="com.whatsberry.xmpp.ImageViewerActivity"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen"
            android:exported="false"/>

        <activity
            android:name="com.whatsberry.xmpp.VideoPlayerActivity"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen"
            android:exported="false"/>

        <activity
            android:name="com.whatsberry.xmpp.VoicePlayerActivity"
            android:theme="@android:style/Theme.Light"
            android:exported="false"/>

        <!-- Background service for XMPP connection -->
        <service
            android:name="com.whatsberry.xmpp.XMPPService"
            android:enabled="true"
            android:exported="false"/>

        <!-- Boot receiver to start service on device boot -->
        <receiver
            android:name="com.whatsberry.xmpp.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>
    </application>
</manifest>