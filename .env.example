# WhatsBerry Server Configuration
# Copy this file to .env and customize for your setup
#
# NOTE: This file is primarily for docker-compose.yml (full stack)
# For hybrid setup (recommended), see docker-compose-hybrid.yml and SERVER_SETUP.md

# ========================================
# Domain Configuration
# ========================================

# Your server's domain name (must be accessible from the internet)
DOMAIN=whatsberry.descarga.media

# Admin email/JID
ADMIN_JID=admin@whatsberry.descarga.media

# ========================================
# Slidge WhatsApp Gateway Configuration
# ========================================

# Gateway component JID (leave as is unless you know what you're doing)
SLIDGE_JID=whatsapp.localhost

# Component secret (CHANGE THIS to a strong random string!)
# Generate with: openssl rand -base64 32
SLIDGE_SECRET=your-secret-change-this-to-random-string

# ========================================
# Prosody Configuration
# ========================================

# Prosody internal port (no TLS, proxy handles STARTTLS)
PROSODY_PORT=5200

# Prosody HTTP port (for file uploads)
PROSODY_HTTP_PORT=5280

# ========================================
# XMPP STARTTLS Proxy Configuration
# ========================================

# Public XMPP port (with STARTTLS for BB10 compatibility)
XMPP_PROXY_PORT=5222
